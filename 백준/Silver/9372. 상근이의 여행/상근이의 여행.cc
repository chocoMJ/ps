#include <iostream>

using namespace std;

int main() {
	int T;
	cin >> T;
	int N, M;

	while (T--) {
		cin >> N >> M;
		while (M--) {
			int a, b;
			cin >> a >> b;
		}
		cout << N - 1 << endl;
	}

	return 0;
}